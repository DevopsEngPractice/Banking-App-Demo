apiVersion: v1
kind: Service

metadata:
  name: services-service
  namespace: banking-app-dev
  labels: 
    app: services-service

spec:
  selector:
    app: services-dep

  ports:
    - name: services-service
      port: 5003
      targetPort: 5003

  type: ClusterIP