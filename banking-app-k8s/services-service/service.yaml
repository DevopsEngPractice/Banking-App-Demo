apiVersion: v1
kind: Service

metadata:
  name: services-service
  namespace: banking-app-dev

spec:
  selector:
    app: services-dep

  ports:
    - port: 5001
      targetPort: 5001

  type: ClusterIP