apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: banking-app-dev
  labels: 
    app: frontend

spec:
  selector:
    app: frontend

  ports:
    - name: frontend
      port: 80
      targetPort: 8080

  type: LoadBalancer