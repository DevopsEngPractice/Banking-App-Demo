apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: banking-app-dev
  labels: 
    app: auth-service

spec:
  selector:
    app: auth-deployment

  ports:
    - name: auth-service
      port: 5001
      targetPort: 5001

  type: ClusterIP